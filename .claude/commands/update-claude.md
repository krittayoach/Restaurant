Review the conversation history and update CLAUDE.md.

1. List changes made in this session grouped by category:
   - ✅ Redesign / UI
   - ✅ Features
   - ✅ Bug fixes
   - ✅ Config / Setup

2. Show the list and wait for user confirmation.

3. After confirmation, update **only** the `## สถานะปัจจุบัน` section in CLAUDE.md:
   - Move completed items into `### ✅ เสร็จแล้ว`
   - Keep remaining work in `### ⏳ ยังไม่ได้ทำ`
   - Update the date `(YYYY-MM-DD)` in the section header
   - Do not modify any other section
